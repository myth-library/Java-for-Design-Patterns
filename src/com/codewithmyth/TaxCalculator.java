package com.codewithmyth;

public interface TaxCalculator {
    // Method declaration (these method doesn't have body)
    float calculateTax();
        /* This method declaration says, if you want to create a TaxCalculator class
           that class should have a method with this signature */

    /* if we want work with TaxCalculator2019 or TaxCalculator2020, we should work with
       this interface */
}
